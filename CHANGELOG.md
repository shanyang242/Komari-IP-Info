# Changelog

## 0.0.1 - 2026-09-10

- IPv6 规范化不再依赖全局 URL，兼容 Komari 实际 JS 运行时。
- 拦截 Komari net.isIP 判定为 IPv4 的映射 IPv6，测试不再注入全局 URL。
- 添加可直接在 Komari 源码中运行的 IPv6 路由回归验证。

- 修复无效 Net.Coffee 响应被缓存的问题，校验返回 IP 与国家字段；缓存结构更新为 6。
- 基础查询、降级查询和全球延迟共用 25 秒总预算。
- 规范化 IPv6 并排除本地、映射、文档和特殊用途地址。
- 延迟刷新使用旧缓存时仍明确返回失败提示。

- 提供 IPv4 / IPv6、地理信息、ASN、运营商和网络类型。
- 显示原生 IP、广播 IP 或任播 IP 标签。
- 提供六个全球节点的按需延迟检测。
- Net.Coffee 作为基础信息主源，proxycheck.io 和 ipapi.is 作为降级源。
- 基础查询复用原生性结果，全球延迟正常情况下只请求一次 Ping 接口。
- 中国大陆 IP 在全球 Ping 前排除。
- 添加缓存、旧缓存降级、5 分钟失败缓存、限流和并发合并。
- 使用原子方式保存缓存，并清理过期的调用方限流记录。
- 游客请求在访问第三方数据源前返回 `403`；只有已登录管理员会触发 IP 检测。
- 主题可强制刷新当前选中的 IPv4 或 IPv6，并同步更新该地址的基础信息和六地延迟。
- 不使用系统 RPC、远程执行或额外高风险权限。
